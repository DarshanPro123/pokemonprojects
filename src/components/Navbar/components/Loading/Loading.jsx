// Loading.jsx
import React from "react";
import "./Loading.css"; // Optional: Add some styles for the loading screen

const Loading = () => {
  return (
    <div className="loading">
      <p>Loading...</p>
    </div>
  );
};

export default Loading;
